import React,{useState}from"react";import{useAuth}from"../lib/AuthContext";import{Button,Field}from"./UI";export default function AuthModal({close}){const{login,signup}=useAuth();const[mode,setMode]=useState("login"),[username,setUsername]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError("");try{mode==="login"?await login(username,password):await signup(username,email,password);close()}catch(e){setError(e.message)}finally{setBusy(false)}}return <div className="modalBackdrop" onMouseDown={close}><form className="modal" onMouseDown={e=>e.stopPropagation()} onSubmit={submit}><div className="tabs"><button type="button" className={mode==="login"?"active":""} onClick={()=>setMode("login")}>Log in</button><button type="button" className={mode==="signup"?"active":""} onClick={()=>setMode("signup")}>Sign up</button></div><Field label="Username" value={username} onChange={e=>setUsername(e.target.value)} required/>{mode==="signup"&&<Field label="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/>}<Field label="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={mode==="signup"?8:1} required/>{error&&<div className="error">{error}</div>}<Button disabled={busy}>{busy?"Working…":mode==="login"?"Log in":"Create account"}</Button></form></div>}
