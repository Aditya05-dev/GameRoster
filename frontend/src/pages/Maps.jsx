import React,{useEffect,useState}from"react";import{useSearchParams}from"react-router-dom";import{api}from"../lib/api";import{Card,Empty,Select}from"../components/UI";export default function Maps(){const[sp]=useSearchParams(),[maps,setMaps]=useState([]),[selected,setSelected]=useState(""),[markers,setMarkers]=useState([]);const gameId=sp.get("gameId");useEffect(()=>{api.get(`/maps${gameId?`?gameId=${gameId}`:""}`,{auth:false}).then(r=>{setMaps(r.maps||[]);if(r.maps?.[0])setSelected(r.maps[0].id)})},[gameId]);useEffect(()=>{if(selected)api.get(`/maps/${selected}/markers`,{auth:false}).then(r=>setMarkers(r.markers||[]))},[selected]);const map=maps.find(m=>m.id===selected);return <section className="section"><h1>Interactive Maps</h1>{maps.length?<><Select label="Map" value={selected} onChange={e=>setSelected(e.target.value)}>{maps.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</Select><Card className="mapCanvas">{map?.image_url&&<img src={map.image_url} alt={map.name}/>} {!map?.image_url&&<div className="mapPlaceholder">Upload/configure a licensed map image</div>}{markers.map(m=><button title={`${m.label}${m.notes?` — ${m.notes}`:""}`} className="marker" key={m.id} style={{left:`${m.x}%`,top:`${m.y}%`}}>{m.category?.[0]||"•"}</button>)}</Card></>:<Empty title="No maps configured" body="The map and marker APIs are ready. Add licensed images and reviewed markers from admin/moderator tools."/>}</section>}
