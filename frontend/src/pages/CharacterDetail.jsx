import React,{useEffect,useState}from"react";import{useParams}from"react-router-dom";import{api}from"../lib/api";import{Badge,Card,Empty}from"../components/UI";export default function CharacterDetail(){const{id}=useParams();const[c,setC]=useState(null),[builds,setBuilds]=useState([]);useEffect(()=>{api.get(`/characters/${id}`,{auth:false}).then(r=>setC(r.character));api.get(`/builds?characterId=${id}`,{auth:false}).then(r=>setBuilds(r.builds||[])).catch(()=>{})},[id]);if(!c)return <section className="section">Loading…</section>;return <section className="section"><div className="characterHero"><div><p className="eyebrow">CHARACTER</p><h1>{c.name}</h1><div className="badgeRow">{c.rarity&&<Badge>{c.rarity}★</Badge>}{c.tier&&<Badge>{c.tier} Tier</Badge>}{Object.entries(c.stats||{}).slice(0,4).map(([k,v])=><Badge key={k}>{v}</Badge>)}</div></div>{c.portrait_url&&<img src={c.portrait_url} alt={c.name}/>}</div><div className="twoCol"><div><h2>Strengths</h2><Card>{c.strengths?.length?<ul>{c.strengths.map(x=><li key={x}>{x}</li>)}</ul>:"No reviewed notes yet."}</Card><h2>Gameplay</h2><Card>{c.gameplay_notes||"No reviewed gameplay notes yet."}</Card></div><div><h2>Weaknesses</h2><Card>{c.weaknesses?.length?<ul>{c.weaknesses.map(x=><li key={x}>{x}</li>)}</ul>:"No reviewed notes yet."}</Card><h2>Skills</h2>{c.skills?.length?c.skills.map(s=><Card key={s.id||s.name}><b>{s.name}</b><p>{s.type}</p><small>{s.description}</small></Card>):<Empty title="No skills" body="Skills have not been reviewed and published yet."/>}</div></div><h2>Community builds</h2>{builds.length?<div className="list">{builds.map(b=><Card key={b.id}><b>{b.title}</b><p>{b.build_type} • by {b.author_username||"community"}</p></Card>)}</div>:<Empty title="No public builds" body="Be the first to publish one after signing in."/>}</section>}
